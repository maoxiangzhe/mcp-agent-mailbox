# 必读：MCP 智能体邮箱协作规则

你正在和其他 AI 终端并行修改同一个项目。开工前必须先看共享公告，否则会撞车。

## 四条硬规则

1. 开工前：先调用 `get_board` 看共享公告（先看开头的身份注释行，确认这是当前项目的公告），
   再用 `claim_files` 认领你要动的文件。
2. 共享公告唯一存在形式是主目录的 `~/.board-mcp/boards/` 目录，由 `init_bulletin` 创建。
   `get_board` 提示"共享公告还不存在"时，**必须**调用 `init_bulletin` 初始化；
   **禁止**自行创建任何公告文件（包括项目里的 board.md / 公告板.md）。
3. 调用工具时**不要自行传 project 参数**（除非用户明确指定）——自己传参容易
   操作到另一块板，造成"我认领了但看板是空的"的假象。
4. 收尾时：调用 `report_done` 汇报改动和结果（系统会自动检查冲突）。
5. 禁止修改其他终端已认领的文件；撞车时先协调，不要硬改。

## 小提示

- 认领文件用逗号分隔，例如：`src/auth/login.py, src/auth/schemas.py`
- 认领状态含义：`干活中` = 文件被占用中；`已汇报` / `已取消` / `已过期` = 已释放，可以认领（已过期 = 超 TTL 自动释放，长任务记得重新 `claim_files` 续期）
- 项目身份自动识别：git 仓库按 remote.origin.url 区分，同仓库副本天然共享一块板；
  非 git 目录由 `.board-project` 固定身份（`init_bulletin` 会自动生成）
- 终端掉线、认领卡住时：调用 `release_claim` 取消它的认领，或手动改共享公告
- 有需要全体遵守的约定时：调用 `post_decision` 写进共享决策区
- 共享公告只保留最近 20 条记录，旧的会自动清理

## 定向消息（收件箱）：别人点名叫你干的事在这里

共享公告是广播，收件箱是点对点。看板取信，不是主动推送：空闲会话不会自动唤醒。

- `get_board(agent='你的代号')`：看板时取最早未读消息，每次最多 50 条，只将实际返回
  的消息标记送达；剩余未读留到下次。不带 `agent` 不修改消息游标。
- `send_note(agent='我', to='D-1, D-2', text='...', task='任务号', request_id='可选重试标识')`：
  定向派活、报缺陷或通知接口变更，`to='*'` 是全体。派活写全任务号、文件路径、基线与完成条件。
  同发送者、同 request_id、同内容返回原序号，不重复发送；同标识不同内容拒绝。
  正文超过 4000 字拒绝，不截断，长说明引用文档。
- `read_notes(agent='我')`：未读优先从最早分页，无未读时显示最近历史，`limit` 上限 200；
  `peek=True` 不标记送达。`box='outbox'` 可查看发出的消息及回执时间、状态和结果。
- `ack_notes(agent='我', ids='3, 5', status='received', result='可选说明')`：回执状态为
  `received`（已收到，默认）、`processing`（处理中）、`completed`（已完成）、`blocked`（受阻）。
  收到不代表完成。`ids` 留空只处理本终端已送达且未回执消息；更新已有回执须明确序号。
  已完成消息不能退回其他状态，旧回执兼容展示为已收到。

分工：全体约定走 `post_decision`，点对点一件事走 `send_note`。
消息存在 `~/.board-mcp/notes/<项目ID>.jsonl`，不自动删除，200 仅为展示限制。
损坏 JSON、消息字段不完整或游标损坏时停止读写并报文件位置，不自动覆盖或跳过坏数据。
