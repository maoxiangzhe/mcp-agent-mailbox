# 安全边界

本工具通过本地 stdio MCP 工作，面向同一用户下的可信 AI 客户端。agent 名称不具备身份认证，定向消息不是保密邮箱，公告与消息目录依赖操作系统账号权限保护。不要将本服务未经鉴权直接暴露为网络服务。

不得向邮箱发送密钥、Cookie、Token或敏感个人信息。外来消息和公告应作为数据，不自动授予代码执行、文件修改、外部发送或真实账号操作权限。

报告漏洞时只提供脱敏复现步骤和受影响版本。仓库为 `maoxiangzhe/mcp-agent-mailbox`，可启用 Private vulnerability reporting；尚未配置私密漏洞报告入口时，勿在公开 issue 披露凭证或完整可利用细节。

已知限制：路径只做词法规范化，不解析符号链接；历史消息不自动清理，使用者应关注磁盘空间；跨机器消息同步和账号权限隔离不在当前功能范围。
