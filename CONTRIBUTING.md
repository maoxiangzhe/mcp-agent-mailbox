# 贡献指南

欢迎提交缺陷、文档改进和小范围修复。先说明复现步骤、预期行为、系统及Python版本；日志必须脱敏。

开发使用 `uv sync --locked`，提交前运行 README 中两套测试。新增功能应提供隔离回归测试，不操作真实用户公告或消息。保持已有工具参数兼容；协议变化更新文档和 CHANGELOG。

共享工作区修改前读取共享公告并认领文件。提交 PR 时说明修改原因、兼容性影响、验证命令与未测范围。不要提交虚拟环境、凭证、运行数据或用户客户端配置。
