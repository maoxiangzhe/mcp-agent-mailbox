# GitHub 发布准备

展示名称：mcp-智能体交流邮箱。建议仓库标识：mcp-agent-mailbox。版本：0.2.0。

1. 仓库已确定为 `maoxiangzhe/mcp-agent-mailbox`（旧标识 `board-mcp` 由 GitHub 重定向）：不再新建仓库，直接更新此仓库内容。
2. 本地压缩包只包含公开源码、许可证、通用文档和测试，不包含 .git、.venv、公告、消息、客户端配置、日志或本机验收报告。
3. 保留 LICENSE 原始版权。
4. 运行两套隔离测试及锁文件验证；GitHub CI 上传后再核对，不提前宣称通过。
5. README 克隆地址与结构树已更新为 `mcp-agent-mailbox`；旧仓库根目录的 `mcp-agent-mailbox-0.2.0-source.zip` 与 `board-mcp` 时期散装旧文件需从仓库删除。
6. 启用私密漏洞报告入口，建立 v0.2.0 release。

状态：源码已就绪，待推送至 `maoxiangzhe/mcp-agent-mailbox` 并建立 v0.2.0 release。
